# SQL_AGENT
AI-powered SQL agent that converts natural language into secure, optimized SQL queries using Ollama (LLaMA 3). Features interactive Streamlit dashboards, CSV-to-database import, query history, charts, and a FastAPI backend with full SQL safety enforcement.
